# Shared Package

Shared TypeScript capture contracts and Zod validation schemas.

The server contract supports only `link`, `text` and `image`. Image captures must reference object storage; raw data URLs are rejected.